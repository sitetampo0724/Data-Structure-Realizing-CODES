So what exactly is the use of recursive elimination?  
1. It supports processing only the stack area content, unlike system-level multitasking.  
2. It does not use the system's stack space but directly utilizes system memory.